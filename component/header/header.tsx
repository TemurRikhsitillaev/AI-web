import UserDropdown from "@/component/user-dropdown";

const Header = () => (
  <header>
    <h1>Header</h1>
    <p>This is a header component</p>
    <UserDropdown />
  </header>
);

export default Header;
