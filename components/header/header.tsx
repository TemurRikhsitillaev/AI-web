import UserDropdown from "../user-dropdown/user-dropdown";

const Header = () => (
  <header className="px-5 py-3 flex items-end justify-end">
    <UserDropdown />
  </header>
);

export default Header;
